# An AWS SQS client written using alpakka 

Run the application using `sbt run`. 

It reads published messages from a SQS queue running on local-stack. 

## Pre-requisite. 

- Use the docker-compose.yml file and start local stack. 